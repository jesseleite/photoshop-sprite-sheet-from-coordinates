#target photoshop#include "underscore.js"app.preferences.rulerUnits = Units.PIXELS;var document = activeDocument;var activeLayer = document.activeLayer;var spriteX = document.width;var spriteY = document.height;document.resizeCanvas(5*spriteX, 5*spriteY, AnchorPosition.TOPLEFT);const getMoveableLayers = function (layers) {    var moveableLayers = [];    for (i=0; i < layers.length; i++) {        if (layers[i].name.match(/[0-9],[0-9]/)) {            moveableLayers.push(layers[i]);        }    }    return moveableLayers;}var moveableLayers = getMoveableLayers(document.layers);for (i=0; i < moveableLayers.length; i++) {    translateX = moveableLayers[i].name.substring(0, 1) * spriteX;    translateY = moveableLayers[i].name.substring(2, 3) * spriteY;    moveableLayers[i].visible = 1;    moveableLayers[i].translate(translateX, translateY);}